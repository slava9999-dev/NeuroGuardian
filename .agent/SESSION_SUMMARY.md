# Сессия отладки Sentinel - 04.01.2026

## Что было сделано

### 🔧 Исправления критических багов

1. **WB Price Fetching (ИСПРАВЛЕНО ✅)**
   - Проблема: Type mismatch между BIGINT из Postgres (приходит как string) и number в JSON от WB API
   - Решение: Добавлен `Number()` cast при сравнении ID товаров
   - Результат: **12/12 цен успешно получено от WB**

2. **WB Price Update (ЧАСТИЧНО ИСПРАВЛЕНО ⚠️)**
   - Проблема: API возвращает "Invalid data format"
   - Попытки: Убрали `discount` поле, изменили структуру payload
   - Статус: **Требует дополнительной отладки** - возможно нужен другой формат или эндпоинт

3. **Ozon API (НЕ РАБОТАЕТ ❌)**
   - Проблема: 404 на всех эндпоинтах (/v1, /v2, /v4)
   - Возможные причины:
     - Неверный формат API ключей
     - Ограничения кабинета продавца
     - Требуется другой метод авторизации
   - Статус: **Требует проверки ключей и документации**

4. **Database Schema (ИСПРАВЛЕНО ✅)**
   - Добавлена колонка `details JSONB` в `sentinel_logs`
   - Создана таблица `price_rules` для умного репрайсинга
   - Миграция выполнена успешно

### 📊 Текущее состояние

**Работает:**

- ✅ Получение цен от WB (12/12 товаров)
- ✅ Обнаружение угроз (цена ниже min_price)
- ✅ Запись в БД
- ✅ Логирование действий

**Не работает:**

- ❌ Обновление цен на WB (ошибка API)
- ❌ Получение цен от Ozon (404)
- ❌ Обновление цен на Ozon

### 🎯 Следующие шаги

1. **WB Price Update**
   - Проверить актуальную документацию WB API
   - Возможно нужен эндпоинт `/api/v2/prices` вместо `/api/v2/upload/task`
   - Попробовать синхронное обновление вместо асинхронного

2. **Ozon Integration**
   - Проверить формат API ключей в БД
   - Убедиться, что `client_id` и `api_key` корректны
   - Попробовать базовый эндпоинт `/v1/product/list`

3. **Production Setup**
   - Настроить cron job на каждые 30 минут
   - Добавить Telegram уведомления о результатах цикла
   - Протестировать полный цикл с реальными данными

### 📝 Созданные файлы

- `scripts/apply-migrations.ts` - Миграция БД
- `scripts/check-db-schema.ts` - Проверка схемы
- `scripts/debug-sentinel-run.ts` - Отладочный запуск для одного пользователя
- `scripts/diagnose-sentinel.ts` - Диагностика данных
- `scripts/run-sentinel-cycle.ts` - Боевой запуск полного цикла

### 🐛 Известные проблемы

1. WB API возвращает "Invalid data format" при попытке обновить цену
2. Ozon API возвращает 404 на все эндпоинты
3. Нужно добавить обработку ошибок в `rulesMap` (сейчас не используется)

### 💡 Рекомендации

- Запустить `npx tsx scripts/run-sentinel-cycle.ts` для полного теста
- Проверить логи WB API на предмет подсказок о формате
- Связаться с поддержкой Ozon для уточнения доступа к API
